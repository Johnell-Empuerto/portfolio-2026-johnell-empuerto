const prefersReducedMotion = window.matchMedia(
    "(prefers-reduced-motion: reduce)",
  ).matches,
  reducedMotion = () => prefersReducedMotion;
function splitTitle() {
  document.querySelectorAll(".split-text").forEach((e) => {
    let t = e.textContent.trim();
    ((e.textContent = ""),
      t.split(" ").forEach((r, o) => {
        let a = document.createElement("span");
        ((a.className = "line"),
          (a.textContent = r),
          e.appendChild(a),
          o < t.split(" ").length - 1 &&
            e.appendChild(document.createTextNode(" ")));
      }));
  });
}
function setupCursor() {
  if (reducedMotion() || window.matchMedia("(pointer: coarse)").matches) return;
  let e = document.querySelector(".cursor"),
    t = window.innerWidth / 2,
    r = window.innerHeight / 2,
    o = t,
    a = r;
  window.addEventListener("mousemove", (o) => {
    ((t = o.clientX), (r = o.clientY), (e.style.opacity = "1"));
  });
  let s = () => {
    ((o += (t - o) * 0.18),
      (a += (r - a) * 0.18),
      (e.style.transform = `translate(${o}px, ${a}px) translate(-50%, -50%)`),
      requestAnimationFrame(s));
  };
  (s(),
    document.querySelectorAll("a, button").forEach((t) => {
      (t.addEventListener("mouseenter", () => e.classList.add("is-active")),
        t.addEventListener("mouseleave", () =>
          e.classList.remove("is-active"),
        ));
    }));
}
function setupDeveloperScene() {
  let e = document.querySelector(".developer-canvas");
  if (!window.THREE || !e || reducedMotion()) return;
  let t = document.querySelector(".orb-card"),
    r = new THREE.Scene(),
    o = new THREE.PerspectiveCamera(45, 1, 0.1, 100);
  o.position.z = 5.2;
  let a = new THREE.WebGLRenderer({
    canvas: e,
    alpha: !0,
    antialias: !0,
    powerPreference: "high-performance",
  });
  a.setPixelRatio(Math.min(window.devicePixelRatio, 2));
  let s = new THREE.Group(),
    n = new THREE.Group(),
    i = window.innerWidth > 640 ? 76 : 48,
    l = [],
    c = new Float32Array(3 * i),
    d = new Float32Array(3 * i);
  for (let $ = 0; $ < i; $ += 1) {
    let p = 1.95 + 0.8 * Math.random(),
      u = 1.15 + 0.55 * Math.random(),
      y = 0.7 + 0.55 * Math.random(),
      g = Math.random() * Math.PI * 2,
      _ = Math.acos(2 * Math.random() - 1);
    ((c[3 * $] = p * Math.sin(_) * Math.cos(g)),
      (c[3 * $ + 1] = u * Math.sin(_) * Math.sin(g)),
      (c[3 * $ + 2] = y * Math.cos(_)));
    let m = new THREE.Color(58879),
      f = new THREE.Color(16731853),
      h = m.lerp(f, Math.random());
    ((d[3 * $] = h.r),
      (d[3 * $ + 1] = h.g),
      (d[3 * $ + 2] = h.b),
      l.push({ x: c[3 * $], y: c[3 * $ + 1], z: c[3 * $ + 2] }));
  }
  let x = new THREE.BufferGeometry();
  (x.setAttribute("position", new THREE.BufferAttribute(c, 3)),
    x.setAttribute("color", new THREE.BufferAttribute(d, 3)));
  let v = new THREE.PointsMaterial({
      size: 0.045,
      vertexColors: !0,
      transparent: !0,
      opacity: 0.95,
      depthWrite: !1,
    }),
    w = new THREE.Points(x, v);
  n.add(w);
  let b = [];
  for (let S = 0; S < l.length; S += 1)
    for (let A = S + 1; A < l.length; A += 1) {
      let k = l[S].x - l[A].x,
        E = l[S].y - l[A].y,
        L = l[S].z - l[A].z,
        q = Math.sqrt(k * k + E * E + L * L);
      q < 1.25 && b.push(l[S].x, l[S].y, l[S].z, l[A].x, l[A].y, l[A].z);
    }
  let T = new THREE.BufferGeometry();
  T.setAttribute("position", new THREE.Float32BufferAttribute(b, 3));
  let M = new THREE.LineBasicMaterial({
      color: 6809849,
      transparent: !0,
      opacity: 0.28,
      depthWrite: !1,
    }),
    P = new THREE.LineSegments(T, M);
  n.add(P);
  let C = new THREE.Mesh(
    new THREE.IcosahedronGeometry(1.08, 1),
    new THREE.MeshBasicMaterial({
      color: 8150271,
      wireframe: !0,
      transparent: !0,
      opacity: 0.68,
    }),
  );
  n.add(C);
  let R = new THREE.Mesh(
      new THREE.TorusGeometry(1.62, 0.006, 8, 120),
      new THREE.MeshBasicMaterial({
        color: 58879,
        transparent: !0,
        opacity: 0.42,
      }),
    ),
    B = R.clone();
  ((B.material = R.material.clone()),
    B.material.color.set(16731853),
    (B.material.opacity = 0.32),
    (R.rotation.x = Math.PI / 2.2),
    (B.rotation.y = Math.PI / 2.4),
    n.add(R, B),
    s.add(n),
    r.add(s));
  let Y = 0,
    O = 0;
  (t?.addEventListener("mousemove", (e) => {
    let r = t.getBoundingClientRect();
    ((Y = ((e.clientX - r.left) / r.width - 0.5) * 0.9),
      (O = ((e.clientY - r.top) / r.height - 0.5) * 0.9));
  }),
    t?.addEventListener("mouseleave", () => {
      ((Y = 0), (O = 0));
    }));
  let z = new THREE.Clock();
  function X() {
    let r = t?.getBoundingClientRect() || e.getBoundingClientRect(),
      s = Math.max(1, r.width),
      n = Math.max(1, r.height);
    ((o.aspect = s / n),
      o.updateProjectionMatrix(),
      a.setPixelRatio(Math.min(window.devicePixelRatio, 2)),
      a.setSize(s, n, !1));
  }
  (!(function e() {
    let t = z.getElapsedTime();
    ((n.rotation.y = 0.22 * t),
      (n.rotation.x = 0.18 * Math.sin(0.35 * t)),
      (C.rotation.x = 0.42 * t),
      (C.rotation.y = 0.32 * t),
      (R.rotation.z = 0.38 * t),
      (B.rotation.x = Math.PI / 2.4 + 0.28 * t),
      (s.rotation.y += (Y - s.rotation.y) * 0.045),
      (s.rotation.x += (O - s.rotation.x) * 0.045),
      a.render(r, o),
      requestAnimationFrame(e));
  })(),
    X(),
    window.addEventListener("resize", X));
}
function setupAnimations() {
  if (!window.gsap || !window.ScrollTrigger || reducedMotion()) {
    document.querySelectorAll(".reveal").forEach((e) => {
      ((e.style.opacity = "1"), (e.style.transform = "none"));
    });
    return;
  }
  (gsap.registerPlugin(ScrollTrigger, ScrollToPlugin),
    gsap.to(".scroll-progress", {
      width: "100%",
      ease: "none",
      scrollTrigger: { start: "top top", end: "bottom bottom", scrub: !0 },
    }),
    gsap.utils.toArray(".reveal:not(.skill-card)").forEach((e) => {
      gsap.to(e, {
        opacity: 1,
        y: 0,
        duration: 1,
        ease: "power3.out",
        scrollTrigger: {
          trigger: e,
          start: "top 86%",
          toggleActions: "play none none reverse",
        },
      });
    }));
  let e = document.querySelector(".hero-title"),
    t = gsap.utils.toArray(".hero-title .line");
  (e &&
    t.length &&
    gsap
      .timeline({ delay: 0.18 })
      .set(e, {
        "--shine-x": "-145%",
        "--shine-opacity": 0,
        "--title-glow": 0.38,
      })
      .from(t, {
        yPercent: 118,
        opacity: 0,
        rotateX: -42,
        filter: "blur(14px)",
        transformOrigin: "left bottom",
        duration: 1.18,
        stagger: 0.13,
        ease: "power4.out",
      })
      .to(
        e,
        { "--shine-opacity": 0.82, duration: 0.16, ease: "power2.out" },
        "-=0.58",
      )
      .to(
        e,
        {
          "--shine-x": "145%",
          "--title-glow": 1,
          duration: 1.12,
          ease: "power2.inOut",
        },
        "<",
      )
      .to(
        e,
        { "--shine-opacity": 0, duration: 0.28, ease: "power2.out" },
        "-=0.1",
      )
      .to(
        t,
        {
          backgroundPosition: "100% 50%",
          duration: 5.5,
          repeat: -1,
          yoyo: !0,
          ease: "sine.inOut",
        },
        "-=0.4",
      )
      .to(
        e,
        {
          "--title-glow": 0.62,
          duration: 2.8,
          repeat: -1,
          yoyo: !0,
          ease: "sine.inOut",
        },
        "<",
      ),
    gsap.from(".site-header", {
      y: -24,
      opacity: 0,
      duration: 0.9,
      ease: "power3.out",
    }),
    gsap.utils.toArray(".dev-chip").forEach((e, t) => {
      gsap.to(e, {
        y: t % 2 == 0 ? 12 : -10,
        x: t % 2 == 0 ? 6 : -6,
        rotate: t % 2 == 0 ? 4 : -4,
        duration: 3.8 + 0.35 * t,
        repeat: -1,
        yoyo: !0,
        ease: "sine.inOut",
        delay: 0.18 * t,
      });
    }),
    gsap.utils.toArray(".project-card").forEach((e, t) => {
      gsap.from(e.querySelector(".project-visual"), {
        scale: 0.94,
        rotate: -2,
        opacity: 0.65,
        duration: 0.9,
        ease: "power3.out",
        scrollTrigger: { trigger: e, start: "top 72%", scrub: 0.8 },
        delay: 0.08 * t,
      });
    }));
  let r = document.querySelector(".process-steps"),
    o = document.querySelector(".process-card");
  (r &&
    o &&
    gsap.to(r, {
      y: -30,
      ease: "none",
      scrollTrigger: {
        trigger: o,
        start: "top bottom",
        end: "bottom top",
        scrub: !0,
      },
    }),
    gsap.utils.toArray(".count").forEach((e) => {
      let t = Number(e.dataset.count);
      gsap.to(e, {
        innerText: t,
        duration: 1.6,
        snap: { innerText: 1 },
        ease: "power2.out",
        scrollTrigger: { trigger: e, start: "top 82%", once: !0 },
      });
    }));
}
function setupStoryGallery() {
  let e = document.querySelector("[data-gallery-story]");
  if (!e) return;
  let t = window.gsap
      ? gsap.utils.toArray(e.querySelectorAll("[data-gallery-slide]"))
      : Array.from(e.querySelectorAll("[data-gallery-slide]")),
    r = window.gsap
      ? gsap.utils.toArray(e.querySelectorAll("[data-gallery-dot]"))
      : Array.from(e.querySelectorAll("[data-gallery-dot]")),
    o = window.gsap
      ? gsap.utils.toArray(e.querySelectorAll(".story-line"))
      : Array.from(e.querySelectorAll(".story-line")),
    a = e.querySelector(".story-tags"),
    s = e.querySelector("[data-gallery-count]"),
    n = e.querySelector("[data-gallery-caption]"),
    i = e.querySelector("[data-gallery-track]");
  if (!t.length) return;
  let l = -1,
    c = t.length - 1,
    d = (e) => Math.max(0, Math.min(c, e)),
    $ = (e) => Math.max(0, Math.min(1, e)),
    p = (e, o = c ? e / c : 0) => {
      let a = d(e);
      (i && i.style.setProperty("--progress", `${8 + 84 * $(o)}%`),
        a !== l &&
          ((l = a),
          t.forEach((e, t) => {
            e.classList.toggle("is-active", t === l);
          }),
          r.forEach((e, t) => {
            e.classList.toggle("is-active", t === l);
          }),
          s &&
            (s.textContent = `${String(l + 1).padStart(2, "0")} / ${String(t.length).padStart(2, "0")}`),
          n && (n.textContent = t[l].dataset.caption || "")));
    };
  if (!window.gsap || !window.ScrollTrigger || reducedMotion()) {
    (t.forEach((e, t) => {
      ((e.style.opacity = 0 === t ? "1" : "0"),
        (e.style.visibility = 0 === t ? "visible" : "hidden"),
        (e.style.transform = "none"));
    }),
      p(0, 0));
    return;
  }
  (gsap.set(t, {
    autoAlpha: 0,
    yPercent: -78,
    scale: 0.92,
    rotate: -5,
    filter: "blur(10px)",
  }),
    gsap.set(t[0], {
      autoAlpha: 1,
      yPercent: 0,
      scale: 1,
      rotate: 0,
      filter: "blur(0px)",
      zIndex: 2,
    }),
    gsap.fromTo(
      o,
      { autoAlpha: 0, y: 36, filter: "blur(10px)" },
      {
        autoAlpha: 1,
        y: 0,
        filter: "blur(0px)",
        duration: 0.9,
        stagger: 0.16,
        ease: "power3.out",
        scrollTrigger: {
          trigger: e,
          start: "top 72%",
          toggleActions: "play none none reverse",
        },
      },
    ),
    a &&
      gsap.fromTo(
        a.children,
        { autoAlpha: 0, y: 18 },
        {
          autoAlpha: 1,
          y: 0,
          duration: 0.7,
          stagger: 0.07,
          ease: "power3.out",
          scrollTrigger: {
            trigger: a,
            start: "top 88%",
            toggleActions: "play none none reverse",
          },
        },
      ));
  let u = e.querySelector(".story-frame");
  u &&
    gsap.fromTo(
      u,
      { y: 30, rotateX: 5, scale: 0.96 },
      {
        y: 0,
        rotateX: 0,
        scale: 1,
        duration: 1.1,
        ease: "power3.out",
        scrollTrigger: {
          trigger: u,
          start: "top 84%",
          toggleActions: "play none none reverse",
        },
      },
    );
  let y = gsap.timeline({ paused: !0, defaults: { ease: "power2.inOut" } });
  t.forEach((e, t) => {
    gsap.set(e, { zIndex: t + 1 });
  });
  for (let g = 0; g < t.length - 1; g += 1) {
    let _ = t[g],
      m = t[g + 1],
      f = _.querySelector("img"),
      h = m.querySelector("img");
    (y
      .set(m, { zIndex: g + 3 }, g)
      .to(
        _,
        {
          yPercent: 76,
          scale: 0.9,
          rotate: 5,
          autoAlpha: 0,
          filter: "blur(10px)",
          duration: 1,
        },
        g,
      )
      .fromTo(
        m,
        {
          yPercent: -78,
          scale: 0.92,
          rotate: -5,
          autoAlpha: 0,
          filter: "blur(10px)",
        },
        {
          yPercent: 0,
          scale: 1,
          rotate: 0,
          autoAlpha: 1,
          filter: "blur(0px)",
          duration: 1,
          immediateRender: !1,
        },
        g,
      ),
      f && y.to(f, { yPercent: 7, scale: 1.04, duration: 1 }, g),
      h &&
        y.fromTo(
          h,
          { yPercent: -6, scale: 1.08 },
          {
            yPercent: 0,
            scale: 1,
            duration: 1,
            ease: "power2.out",
            immediateRender: !1,
          },
          g,
        ));
  }
  p(0, 0);
  let x = window.matchMedia("(min-width: 981px)").matches;
  (ScrollTrigger.create({
    trigger: e,
    start: x ? "top top" : "top 68%",
    end: x ? `+=${105 * Math.max(t.length - 1, 1)}%` : "bottom top",
    scrub: !x || 0.72,
    pin: x,
    animation: y,
    anticipatePin: 1,
    invalidateOnRefresh: !0,
    onUpdate(e) {
      let t = $(e.progress);
      p(Math.round(t * c), t);
    },
    onLeave: () => p(c, 1),
    onLeaveBack: () => p(0, 0),
  }),
    e.querySelectorAll("img").forEach((e) => {
      e.addEventListener(
        "load",
        () => {
          ScrollTrigger.refresh();
        },
        { once: !0 },
      );
    }));
}
function setupStackStoryboard() {
  let e = document.querySelector(".skills-section"),
    t = e?.querySelector(".skills-grid"),
    r = window.gsap ? gsap.utils.toArray(".skills-grid .skill-card") : [],
    o =
      window.matchMedia("(pointer: coarse)").matches ||
      window.matchMedia("(hover: none)").matches;
  if (!e || !t) return;
  if (!window.gsap || !window.ScrollTrigger || reducedMotion() || !r.length) {
    e.classList.add("is-stack-revealed");
    return;
  }
  let a = window.matchMedia("(min-width: 981px)").matches;
  if (!a || o) {
    (e.classList.add("is-stack-revealed"),
      e.classList.remove("is-stack-story"),
      gsap.set(r, {
        opacity: 1,
        x: 0,
        y: 0,
        rotateX: 0,
        rotateY: 0,
        rotateZ: 0,
        scale: 1,
        "--stack-front-opacity": 0,
        "--stack-content-opacity": 1,
        "--stack-content-y": "0px",
      }));
    return;
  }
  (e.classList.add("is-stack-story"), e.classList.remove("is-stack-revealed"));
  let s = [
    { x: -34, y: -18, rotateY: -18, rotateZ: -3.8 },
    { x: 0, y: -30, rotateY: 10, rotateZ: 2.4 },
    { x: 36, y: -16, rotateY: 20, rotateZ: 4.2 },
    { x: -38, y: 18, rotateY: -22, rotateZ: 4 },
    { x: 0, y: 32, rotateY: -10, rotateZ: -2.2 },
    { x: 40, y: 18, rotateY: 18, rotateZ: -4.4 },
  ];
  (gsap.set(e, { "--stack-gap": "0px", "--stack-card-radius": "14px" }),
    gsap.set(r, {
      opacity: 1,
      x: 0,
      y: 0,
      rotateX: 0,
      rotateY: 0,
      rotateZ: 0,
      scale: 1,
      transformOrigin: "center center",
      "--stack-front-opacity": 1,
      "--stack-front-rotate": "0deg",
      "--stack-content-opacity": 0,
      "--stack-content-y": "24px",
    }),
    gsap
      .timeline({
        scrollTrigger: {
          trigger: e,
          start: "top top",
          end: "+=155%",
          scrub: 1,
          pin: !0,
          anticipatePin: 1,
          invalidateOnRefresh: !0,
          onUpdate(t) {
            e.classList.toggle("is-stack-revealed", t.progress > 0.985);
          },
          onLeave: () => e.classList.add("is-stack-revealed"),
          onEnterBack: () => e.classList.remove("is-stack-revealed"),
          onRefresh(t) {
            e.classList.toggle("is-stack-revealed", t.progress > 0.985);
          },
        },
      })
      .to(
        e,
        {
          "--stack-gap": "18px",
          "--stack-card-radius": "24px",
          duration: 0.38,
          ease: "power2.inOut",
        },
        0,
      )
      .to(
        r,
        {
          x: (e) => s[e]?.x || 0,
          y: (e) => s[e]?.y || 0,
          rotateY: (e) => s[e]?.rotateY || 0,
          rotateZ: (e) => s[e]?.rotateZ || 0,
          scale: 0.965,
          duration: 0.38,
          stagger: { each: 0.025, from: "center" },
          ease: "power2.inOut",
        },
        0.08,
      )
      .to(
        r,
        {
          "--stack-front-rotate": "-125deg",
          "--stack-front-opacity": 0.05,
          duration: 0.34,
          stagger: { each: 0.032, from: "center" },
          ease: "power2.in",
        },
        0.28,
      )
      .to(
        r,
        {
          x: 0,
          y: 0,
          rotateX: 0,
          rotateY: 0,
          rotateZ: 0,
          scale: 1,
          duration: 0.5,
          stagger: { each: 0.034, from: "center" },
          ease: "back.out(1.25)",
        },
        0.46,
      )
      .to(
        r,
        {
          "--stack-front-opacity": 0,
          "--stack-content-opacity": 1,
          "--stack-content-y": "0px",
          duration: 0.36,
          stagger: { each: 0.032, from: "center" },
          ease: "power2.out",
        },
        0.56,
      ));
}
function setupScrollTextReveal() {
  let e = document.querySelectorAll(
    ".section-title:not([data-no-scroll-split])",
  );
  e.length &&
    (e.forEach((e) => {
      if (!e.dataset.originalText) {
        let t = e.textContent.replace(/\s+/g, " ").trim();
        ((e.dataset.originalText = t),
          e.setAttribute("aria-label", t),
          e.classList.add("scroll-split"),
          (e.textContent = ""),
          t.split(/(\s+)/).forEach((t) => {
            if (!t.trim()) {
              e.appendChild(document.createTextNode(t));
              return;
            }
            let r = document.createElement("span"),
              o = document.createElement("span"),
              a = document.createElement("span");
            ((r.className = "scroll-word"),
              (o.className = "scroll-word-base"),
              (a.className = "scroll-word-fill"),
              a.setAttribute("aria-hidden", "true"),
              (o.textContent = t),
              (a.textContent = t),
              r.append(o, a),
              e.appendChild(r));
          }));
      }
      let r = e.querySelectorAll(".scroll-word");
      if (!r.length) return;
      if (!window.gsap || !window.ScrollTrigger || reducedMotion()) {
        r.forEach((e) => e.style.setProperty("--word-fill", "100%"));
        return;
      }
      let o = (e) => {
        let t = r.length,
          o = gsap.utils.clamp(0, 1, e);
        r.forEach((e, r) => {
          let a = gsap.utils.clamp(0, 1, (o * (t + 0.85) - r) / 0.9);
          gsap.set(e, {
            "--word-fill": `${Math.round(1e3 * a) / 10}%`,
            y: 10 * (1 - a),
          });
        });
      };
      (o(0),
        ScrollTrigger.create({
          trigger: e,
          start: "top 88%",
          end: "top 42%",
          invalidateOnRefresh: !0,
          onUpdate: (e) => o(e.progress),
          onRefresh: (e) => o(e.progress),
          onLeave: () => o(1),
          onLeaveBack: () => o(0),
        }));
    }),
    window.ScrollTrigger &&
      !reducedMotion() &&
      requestAnimationFrame(() => ScrollTrigger.refresh()));
}
function setupMagneticButtons() {
  reducedMotion() ||
    document.querySelectorAll(".button, .header-cta").forEach((e) => {
      (e.addEventListener("mousemove", (t) => {
        let r = e.getBoundingClientRect(),
          o = t.clientX - r.left - r.width / 2,
          a = t.clientY - r.top - r.height / 2;
        gsap.to(e, {
          x: 0.16 * o,
          y: 0.22 * a,
          duration: 0.35,
          ease: "power3.out",
        });
      }),
        e.addEventListener("mouseleave", () => {
          gsap.to(e, {
            x: 0,
            y: 0,
            duration: 0.45,
            ease: "elastic.out(1, 0.45)",
          });
        }));
    });
}
function setupMarqueeHover() {
  let e = document.querySelectorAll(".marquee-track span");
  !e.length ||
    reducedMotion() ||
    e.forEach((e) => {
      let t = () => {
          (e.classList.add("is-marquee-hot"),
            window.gsap &&
              (gsap.killTweensOf(e),
              gsap.to(e, {
                y: -8,
                scale: 1.08,
                color: "#ff334e",
                textShadow:
                  "0 0 18px rgba(255, 51, 78, 0.7), 0 0 42px rgba(255, 78, 205, 0.34)",
                "--mark-opacity": 1,
                "--mark-scale": 1,
                duration: 0.34,
                ease: "back.out(1.8)",
                overwrite: !0,
              })));
        },
        r = () => {
          (e.classList.remove("is-marquee-hot"),
            window.gsap &&
              (gsap.killTweensOf(e),
              gsap.to(e, {
                y: 0,
                scale: 1,
                color: "rgba(245, 247, 251, 0.72)",
                textShadow: "0 0 0 rgba(255, 51, 78, 0)",
                "--mark-opacity": 0,
                "--mark-scale": 0,
                duration: 0.38,
                ease: "power3.out",
                overwrite: !0,
              })));
        };
      (e.addEventListener("pointerenter", t),
        e.addEventListener("mouseenter", t),
        e.addEventListener("pointerleave", r),
        e.addEventListener("mouseleave", r));
    });
}
function setupFlyingAccents() {
  let e = document.querySelector(".ambient-stage");
  if (!e || reducedMotion()) return;
  let t = window.innerWidth > 760 ? 16 : 8,
    r = document.createDocumentFragment();
  for (let o = 0; o < t; o += 1) {
    let a = document.createElement("span");
    ((a.className = "flying-spark"),
      a.style.setProperty("--top", `${8 + 84 * Math.random()}%`),
      a.style.setProperty("--size", `${70 + 150 * Math.random()}px`),
      a.style.setProperty("--duration", `${13 + 13 * Math.random()}s`),
      a.style.setProperty("--delay", `${-18 * Math.random()}s`),
      r.appendChild(a));
  }
  e.appendChild(r);
}
function setupArtParallax() {
  if (!window.gsap || !window.ScrollTrigger || reducedMotion()) return;
  let e = window.matchMedia("(min-width: 641px)").matches;
  (gsap.utils.toArray("[data-depth]").forEach((e, t) => {
    let r = Number(e.dataset.depth || 0);
    gsap.to(e, {
      y: r,
      x: t % 2 == 0 ? 0.16 * r : -0.12 * r,
      rotate: t % 2 == 0 ? 7 : -7,
      ease: "none",
      scrollTrigger: {
        trigger: document.body,
        start: "top top",
        end: "bottom bottom",
        scrub: 1.25,
      },
    });
  }),
    e &&
      (gsap.to(".hero-card", {
        yPercent: -8,
        rotate: -1.2,
        ease: "none",
        scrollTrigger: {
          trigger: ".hero",
          start: "top top",
          end: "bottom top",
          scrub: 1,
        },
      }),
      gsap.to(".hero-copy", {
        yPercent: 5,
        ease: "none",
        scrollTrigger: {
          trigger: ".hero",
          start: "top top",
          end: "bottom top",
          scrub: 1,
        },
      })),
    gsap.utils.toArray(".section").forEach((e, t) => {
      gsap.fromTo(
        e,
        { "--section-depth": "0px" },
        {
          "--section-depth": t % 2 == 0 ? "18px" : "-18px",
          ease: "none",
          scrollTrigger: {
            trigger: e,
            start: "top bottom",
            end: "bottom top",
            scrub: 1.5,
          },
        },
      );
    }));
}
function setupCardSpotlights() {
  let e = document.querySelectorAll(
      ".about-panel, .project-card, .experience-card, .education-card, .reference-card, .contact-card, .hero-metrics div",
    ),
    t =
      window.matchMedia("(pointer: fine)").matches &&
      !reducedMotion() &&
      window.gsap;
  e.forEach((e) => {
    (e.addEventListener("pointermove", (r) => {
      let o = e.getBoundingClientRect(),
        a = (r.clientX - o.left) / o.width,
        s = (r.clientY - o.top) / o.height;
      (e.style.setProperty("--mx", `${100 * a}%`),
        e.style.setProperty("--my", `${100 * s}%`),
        t &&
          "touch" !== r.pointerType &&
          gsap.to(e, {
            rotateX: (0.5 - s) * 5,
            rotateY: (a - 0.5) * 6,
            y: -4,
            duration: 0.35,
            ease: "power2.out",
            transformPerspective: 900,
          }));
    }),
      e.addEventListener("pointerleave", () => {
        (e.style.setProperty("--mx", "50%"),
          e.style.setProperty("--my", "50%"),
          t &&
            gsap.to(e, {
              rotateX: 0,
              rotateY: 0,
              y: 0,
              duration: 0.55,
              ease: "elastic.out(1, 0.5)",
            }));
      }));
  });
}
function setupMobileMenu() {
  let e = document.querySelector(".hamburger"),
    t = document.querySelector(".mobile-menu"),
    r = t.querySelectorAll(".mobile-nav a"),
    o = !1;
  function a() {
    ((o = !1),
      e.classList.remove("is-active"),
      t.classList.remove("is-open"),
      (document.body.style.overflow = ""),
      window.gsap && !reducedMotion() && gsap.set(r, { opacity: 0, y: 24 }));
  }
  (e.addEventListener("click", () => {
    o
      ? a()
      : ((o = !0),
        e.classList.add("is-active"),
        t.classList.add("is-open"),
        (document.body.style.overflow = "hidden"),
        !(!window.gsap || reducedMotion()) &&
          (gsap.set(t, { opacity: 1 }),
          gsap.fromTo(
            r,
            { opacity: 0, y: 24 },
            {
              opacity: 1,
              y: 0,
              duration: 0.5,
              stagger: 0.07,
              ease: "power3.out",
            },
          )));
  }),
    r.forEach((e) => {
      e.addEventListener("click", (t) => {
        t.preventDefault();
        let r = e.getAttribute("href");
        (a(),
          window.gsap &&
            gsap.to(window, {
              duration: 0.6,
              scrollTo: { y: r },
              ease: "power3.inOut",
            }));
      });
    }),
    document.addEventListener("click", (r) => {
      !o || t.contains(r.target) || e.contains(r.target) || a();
    }));
}
(document.documentElement.classList.add("js"),
  (document.querySelector("#year").textContent = new Date().getFullYear()),
  splitTitle(),
  setupCursor(),
  setupDeveloperScene(),
  setupFlyingAccents(),
  setupAnimations(),
  setupStackStoryboard(),
  setupScrollTextReveal(),
  setupArtParallax(),
  setupMagneticButtons(),
  setupMarqueeHover(),
  setupCardSpotlights(),
  setupMobileMenu());
